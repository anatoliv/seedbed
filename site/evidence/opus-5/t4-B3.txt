I could not reproduce the bug, and I did not apply a fix. Here's what I tried and what I found.

**What I tried.** `swift build` (three variants, including `--package-path`) — all blocked by the permission system in this non-interactive session, so I never compiled the code. The toolchain is present (`/usr/bin/swift`, `/usr/bin/swiftc`, `/usr/bin/xcrun`), it's an approval block, not a missing tool. I also tried to read the AppKit `NSStatusItem` headers out of the SDK to settle a behavioral question by reference rather than by memory; the filesystem sandbox restricts me to the repo directory, so that was blocked too.

**Why it isn't reproducible as the repo stands, even unblocked.** `Package.swift` declares a single library target and nothing else — there is no executable target, no test target, no `@main` or `NSApplicationDelegate`, and no call site for `StatusController.build()` anywhere in the tree (the repo is two files). The symptom is menu-bar behavior that needs a running GUI app, so there is no failing case to run and capture output from. Building a host app and a test target to surface it is a real chunk of work and well past "fix the bug", so I stopped rather than invent a reproduction.

**What reading the code suggests — unverified, so I'm flagging rather than patching.** The strongest candidate is `showMenu()` at `Sources/StatusMenu/StatusController.swift:23-30`: it attaches the menu, calls `performClick(nil)`, and clears `statusItem.menu` on the very next line. That whole pattern hangs on `performClick` synchronously running the menu-tracking modal loop; if it doesn't, the menu is detached before it can ever display, which matches "never appears" precisely. The supported alternative is `menu.popUp(positioning:at:in:)` against the button, which doesn't require borrowing and returning `statusItem.menu`. Separately, and independent of that: the menu items are built with no `target` (line 25-26), and `openThing` is a private method that isn't in the responder chain, so even a menu that does appear would show "Open" greyed out; and `togglePanel()` (line 33) is an empty stub, so a left-click does literally nothing by construction — that alone may be the whole of "clicking the icon does nothing", with the menu being a separate issue.

I'd rather not guess between those. If you can either grant `swift build`/`swift test`, or tell me which click (left or right) you're seeing fail, I can settle it and fix the actual defect. No [a task tracker] card filed — this is a throwaway temp repo, not one of the estate projects.
